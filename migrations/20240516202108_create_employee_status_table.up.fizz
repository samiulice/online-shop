create_table("employee_status") {
    t.Column("id", "integer", {primary: true})
    t.Column("name", "string", {"default": ""})
    t.Column("description", "string", {"default": ""})
    t.Column("created_at", "timestamptz", {})
    t.Column("updated_at", "timestamptz", {})
}
